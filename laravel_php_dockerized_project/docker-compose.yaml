version: "3.8"

services:
  server:
    image: 'nginx:stable-alpine'
    ports:
      - '8000:80' # host 300 bind to internal 80
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      

  # php:
  # mysql:

  # # utility containers
  # composer:
  # npm:
  # artisan: